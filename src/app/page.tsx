"use client";

import UploadForm from "@/components/UploadForm";
import DisplayCards from "@/components/DisplayCards";

const Home = () => {
  return (
    <div>
      <UploadForm />
      <DisplayCards />
    </div>
  );
};

export default Home;
